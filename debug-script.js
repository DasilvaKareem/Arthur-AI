const checkIframeLoading = () => { console.log("Checking iframe loading status"); const iframes = document.querySelectorAll("iframe"); console.log(`Found ${iframes.length} iframes on page`); iframes.forEach((iframe, i) => { console.log(`Iframe ${i}:`, { src: iframe.src, width: iframe.offsetWidth, height: iframe.offsetHeight, visible: iframe.style.display !== "none", loaded: iframe.contentWindow && iframe.contentWindow.document ? "Yes" : "No" });});};
